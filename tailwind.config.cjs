const config = {
  content: ["./src/**/*.{html,js,svelte,ts}"],
  theme: {
    extend: {
      colors: {
        "primary": "#0848F7",
        "centralise": "#009aec" 
      },
    },
  },

  plugins: [],
};

module.exports = config;