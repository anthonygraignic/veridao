const config = {
  content: ["./src/**/*.{html,js,svelte,ts}"],
  theme: {
    extend: {
      colors: {
        "primary": "#0848F7"
      },
    },
  },

  plugins: [],
};

module.exports = config;